# 2026-02-02：CLI 模式引入（已废弃：仓库已收敛为 CLI Only）

## 目标

本文件仅保留历史记录：仓库已执行 Phase 3，**移除了 MCP Server 与 `Windows.Agent` 内嵌 Legacy CLI**，对外入口收敛为独立 CLI 工程。

最新计划请看：

- `docs/plan/2026-02-02-仅CLI与测试还原计划.md`
- `docs/plan/2026-02-02-独立CLI封装计划.md`

## 说明

- 早期实现位于 `src/Windows.Agent/Cli/`，并通过 `src/Windows.Agent/Program.cs` 分发。
- 该实现已在 CLI Only 收敛中移除，以避免双入口与误用。

## 当前对外入口

- `dotnet run --project src/Windows.Agent.Cli/Windows.Agent.Cli.csproj -- help`

## 验证方式

- `dotnet build -c Release`
- `dotnet run --project src/Windows.Agent.Cli/Windows.Agent.Cli.csproj -- help`
